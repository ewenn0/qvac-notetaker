{
  "name": "qvac-notetaker",
  "version": "0.1.0",
  "description": "Local, private AI notetaker for Windows powered by QVAC SDK",
  "main": "./out/main/index.js",
  "author": "Nikita Manshin",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json",
    "typecheck": "npm run typecheck:node && npm run typecheck:web",
    "start": "electron-vite preview",
    "dev": "electron-vite dev",
    "build": "npm run typecheck && electron-vite build",
    "postinstall": "electron-builder install-app-deps && node scripts/prune-modules.cjs && node scripts/patch-bare-spawn.cjs",
    "slim": "node scripts/prune-modules.cjs",
    "clean": "node scripts/clean.cjs",
    "reinstall": "npm run clean && npm install",
    "build:win": "npm run build && electron-builder --win --config",
    "build:mac": "npm run build && electron-builder --mac --config",
    "build:linux": "npm run build && electron-builder --linux --config"
  },
  "//b4a": "Direct dependency to work around a phantom dependency in bogon@1.3.0 (pulled in transitively via @qvac/sdk -> @qvac/rag -> hyperdht). bogon/index.js does require('b4a') but does not declare b4a in its package.json, so it only resolves when some other package hoists b4a to the top level. npm's tree for the SDK nests every b4a copy under individual consumers and leaves the top level empty, so the bare worker aborts at startup with MODULE_NOT_FOUND: 'b4a'. Declaring it here guarantees a top-level node_modules/b4a that bogon can resolve, deterministically and in packaged builds.",
  "dependencies": {
    "@electron-toolkit/preload": "^3.0.1",
    "@electron-toolkit/utils": "^3.0.0",
    "@qvac/sdk": "^0.12.0",
    "b4a": "^1.8.1",
    "electron-updater": "^6.3.9",
    "marked": "^14.1.3"
  },
  "devDependencies": {
    "@electron-toolkit/tsconfig": "^1.0.1",
    "@eslint/js": "^9.13.0",
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^22.7.4",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.2",
    "electron": "^32.1.2",
    "electron-builder": "^25.0.5",
    "electron-vite": "^2.3.0",
    "eslint": "^9.13.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.14",
    "globals": "^15.11.0",
    "prettier": "^3.3.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.11.0",
    "vite": "^5.4.8",
    "vitest": "^2.1.4"
  },
  "//overrides": "Do NOT stub @qvac/* native packages. The SDK worker (bare runtime) eagerly imports every plugin at boot from server/bare/plugins/index.js, so even unused ones must resolve to real modules. A CJS stub breaks bare's ESM loader and the worker exits with code 1 before IPC comes up. Slimming is done exclusively by scripts/prune-modules.cjs, which removes foreign-platform prebuilds (darwin/linux/ios/android) and source maps — that's still ~50% of the install."
}
